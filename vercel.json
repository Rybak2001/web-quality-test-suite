{
  "framework": null,
  "buildCommand": "npx ng analytics disable && npx ng build --no-progress",
  "outputDirectory": "dist/browser",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
